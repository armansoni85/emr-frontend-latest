import { PersonalDetailForm } from "@src/components";

const ProfilePage = () => {
    return (
        <>
            <PersonalDetailForm isReadOnly={true} />
        </>
    );
};

export default ProfilePage;
